#include "main.h"
/**
 * add - adds two integers and prints the result
 * @m: first integer
 * @n: second integer
 *
 * Return: result of the addition
 */
int add(int m, int n);
{
	int sum = m + n;

	return (sum);
}
